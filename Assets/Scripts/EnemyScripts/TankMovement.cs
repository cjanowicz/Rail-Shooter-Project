using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TankMovement : MonoBehaviour {

    ///TankMovement goes here
    ///I want this to be like a fast moving tank that tries to keep up with you on the ground
    ///Actually that isn't very inspired.
    ///Anyways. Turret is a stationary target that makes an obstacle for you to avoid or shoot
    ///Tank kinda can't move the turret yet
    ///So it could be a ground based drone or it could be something else

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
